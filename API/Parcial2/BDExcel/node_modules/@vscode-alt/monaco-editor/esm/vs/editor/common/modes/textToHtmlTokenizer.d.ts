import { IViewLineTokens } from '../core/lineTokens';
import { TokenizationResult2 } from '../core/token';
import { IState } from '../modes';
export interface IReducedTokenizationSupport {
    getInitialState(): IState;
    tokenize2(line: string, state: IState, offsetDelta: number): TokenizationResult2;
}
export declare function tokenizeToString(text: string, tokenizationSupport?: IReducedTokenizationSupport): string;
export declare function tokenizeLineToHTML(text: string, viewLineTokens: IViewLineTokens, colorMap: string[], startOffset: number, endOffset: number, tabSize: number, useNbsp: boolean): string;
