import './hover.css';
import { IDisposable, Disposable } from '../../../common/lifecycle';
export declare class HoverWidget extends Disposable {
    readonly containerDomNode: HTMLElement;
    readonly contentsDomNode: HTMLElement;
    private readonly _scrollbar;
    constructor();
    onContentsChanged(): void;
}
export declare function renderHoverAction(parent: HTMLElement, actionOptions: {
    label: string;
    iconClass?: string;
    run: (target: HTMLElement) => void;
    commandId: string;
}, keybindingLabel: string | null): IDisposable;
