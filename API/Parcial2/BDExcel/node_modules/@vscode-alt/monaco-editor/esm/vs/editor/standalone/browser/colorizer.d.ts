import { IViewLineTokens } from '../../common/core/lineTokens';
import { ITextModel } from '../../common/model';
import { IModeService } from '../../common/services/modeService';
import { IStandaloneThemeService } from '../common/standaloneThemeService';
export interface IColorizerOptions {
    tabSize?: number;
}
export interface IColorizerElementOptions extends IColorizerOptions {
    theme?: string;
    mimeType?: string;
}
export declare class Colorizer {
    static colorizeElement(themeService: IStandaloneThemeService, modeService: IModeService, domNode: HTMLElement, options: IColorizerElementOptions): Promise<void>;
    static colorize(modeService: IModeService, text: string, mimeType: string, options: IColorizerOptions | null | undefined): Promise<string>;
    static colorizeLine(line: string, mightContainNonBasicASCII: boolean, mightContainRTL: boolean, tokens: IViewLineTokens, tabSize?: number): string;
    static colorizeModelLine(model: ITextModel, lineNumber: number, tabSize?: number): string;
}
