import { Event } from '@vscode-alt/monaco-editor/esm/vs/base/common/event';
import { ISCMService, ISCMProvider, ISCMRepository, ILogService } from '../../interfaces';
export declare class SCMService implements ISCMService {
    private readonly logService;
    private _providerIds;
    private _repositories;
    get repositories(): ISCMRepository[];
    private _selectedRepositories;
    get selectedRepositories(): ISCMRepository[];
    private _onDidChangeSelectedRepositories;
    readonly onDidChangeSelectedRepositories: Event<ISCMRepository[]>;
    private _onDidAddProvider;
    get onDidAddRepository(): Event<ISCMRepository>;
    private _onDidRemoveProvider;
    get onDidRemoveRepository(): Event<ISCMRepository>;
    constructor(logService: ILogService);
    registerSCMProvider(provider: ISCMProvider): ISCMRepository;
    private onDidChangeSelection;
}
