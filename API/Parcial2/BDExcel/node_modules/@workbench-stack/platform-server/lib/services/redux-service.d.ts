import * as Logger from 'bunyan';
import { Observable } from 'redux';
import { IReduxService } from '../interfaces';
export declare class ReduxService implements IReduxService<any> {
    private _logger;
    private _store;
    private _dynamic;
    dispatch: any;
    getState: any;
    subscribe: any;
    replaceReducer: any;
    constructor(logger: Logger);
    [Symbol.observable](): Observable<any>;
    addReducer(key: any, value?: any): void;
    addMiddleware(middleware: any): void;
}
