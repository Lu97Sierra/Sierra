import { AbstractEnvironmentService } from '@workbench-stack/core';
import { ServiceBroker } from 'moleculer';
import * as ILogger from 'bunyan';
export declare class EnvironmentClientService extends AbstractEnvironmentService {
    broker: ServiceBroker;
    private topic;
    constructor(logger: ILogger, broker: ServiceBroker);
    extInitialize(): Promise<void>;
    private callAction;
}
