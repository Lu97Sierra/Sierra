import { IConfigurationService } from '@workbench-stack/core';
import { IDisposable } from '@vscode-alt/monaco-editor/esm/vs/base/common/lifecycle';
import { Disposable } from '@workbench-stack/core';
import { ServiceBroker } from 'moleculer';
export declare class ConfigurationServiceExt extends Disposable implements IDisposable {
    private configurationService;
    private broker;
    private pubsub;
    constructor(configurationService: IConfigurationService, broker: ServiceBroker, pubsub: any);
    private registerListeners;
    private onBroadcastChangeConfiguration;
}
