import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
import { ServiceBroker } from 'moleculer';
import * as core from '@workbench-stack/core';
import { AbstractWorkspaceContextService, IWorkspaceFolderCreationData } from '@workbench-stack/core';
import * as ILogger from 'bunyan';
export declare class WorkspaceClientService extends AbstractWorkspaceContextService {
    private lifecycleService;
    broker: ServiceBroker;
    private topic;
    private startupBarrier;
    constructor(lifecycleService: core.ILifecycleService, logger: ILogger, broker: ServiceBroker);
    extInitialize(): Promise<void>;
    initialize(args: any): any;
    private asyncInitializeWorkspace;
    addFolders(foldersToAdd: IWorkspaceFolderCreationData[], index?: number): Promise<void>;
    removeFolders(foldersToRemove: URI[]): Promise<void>;
    updateFolders(foldersToAdd: IWorkspaceFolderCreationData[], foldersToRemove: URI[], index?: number): Promise<void>;
    private callAction;
}
