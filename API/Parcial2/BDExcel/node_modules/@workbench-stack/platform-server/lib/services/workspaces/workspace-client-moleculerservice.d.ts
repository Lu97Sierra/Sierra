import { Container } from 'inversify';
import { Service, ServiceBroker } from 'moleculer';
import { IClientWorkspaceContextService } from '@workbench-stack/core';
export declare class WorkspaceClientMoleculerService extends Service {
    private _workspaceContextService;
    private startupBarrier;
    private container;
    constructor(broker: ServiceBroker, { container, settings }: {
        container: Container;
    } & {
        settings: any;
    });
    get workspaceContextService(): IClientWorkspaceContextService | Promise<IClientWorkspaceContextService>;
}
