import * as MongoDb from 'mongodb';
import { KeyValueLogProvider, IDbKey } from './key-value-log-provider';
/**
 * Uniform Resource Identifier (URI) http://tools.ietf.org/html/rfc3986.
 * This class is a simple parser which creates the basic component parts
 * (http://tools.ietf.org/html/rfc3986#section-3) with minimal validation
 * and encoding.
 *
 *       mongod://<mongo-server-name>:<port>/<database>/<collection>?name=ferret#nose
 *       \____/   \________________________/\______________________/ \_________/ \__/
 *        |           |                                |                |       |
 *     scheme     authority                           path            query   fragment
 *        |   _________________________________________|__
 *       / \ /                                            \
 *       urn:example:animal:ferret:nose
 */
export declare class MongoDBFileProvider extends KeyValueLogProvider {
    readonly database: MongoDb.Db;
    constructor(scheme: string, db: MongoDb.Db);
    protected getAllKeys(key: IDbKey): Promise<string[]>;
    protected hasKey(key: IDbKey): Promise<boolean>;
    protected getValue(key: IDbKey): Promise<string>;
    protected setValue(key: IDbKey, val: string): Promise<void>;
    protected deleteKey(key: IDbKey): Promise<void>;
}
