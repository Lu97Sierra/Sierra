import { ExtensionMessageCollector } from '@workbench-stack/core';
import { ConfigurationScope, IConfigurationNode, IConfigurationRegistry } from '@workbench-stack/core';
export declare function validateProperties(configuration: IConfigurationNode, collector: ExtensionMessageCollector): void;
export declare function validateProperty(property: string): string;
export declare function getScopes(configurationRegistry: IConfigurationRegistry): {
    [key: string]: ConfigurationScope;
};
