import { IStorageService } from './storage';
import { IWorkspaceContextService, IConfigurationService, IEnvironmentService } from '@workbench-stack/core';
import { CdmLogger } from '@cdm-logger/core';
declare type ILogger = CdmLogger.ILogger;
export interface IStorageSettings {
    storageService: IStorageService;
}
export interface IEnvironmentSettings {
    environmentService: IEnvironmentService;
}
export interface IWorkspaceContextSettings {
    workspaceContext: IWorkspaceContextService;
}
export interface IWorkspaceFolderSettings {
    rootFilePath: string;
    isEmptyWorkspace: boolean;
    logger: ILogger;
}
export interface IConfigurationSettings {
    configurationService: IConfigurationService;
}
export declare type ICoreSettings = IEnvironmentSettings & IStorageSettings;
export {};
