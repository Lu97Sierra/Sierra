"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findAllByTypename = exports.resourcePath = void 0;
const _ = require("lodash");
exports.resourcePath = (resource) => typeof resource === 'string' ? resource : `${resource.scheme}://${resource.path}`;
function findAllByTypename(id, items) {
    let i = 0, found, result = [];
    for (; i < items.length; i++) {
        if (items[i].__typename === id) {
            result.push(items[i]);
        }
        else if (_.isArray(items[i].children)) {
            found = findAllByTypename(id, items[i].children);
            if (found.length) {
                result = result.concat(found);
            }
        }
    }
    return result;
}
exports.findAllByTypename = findAllByTypename;
//# sourceMappingURL=util.js.map