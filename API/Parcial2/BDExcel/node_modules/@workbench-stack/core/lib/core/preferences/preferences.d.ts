import { ConfigurationTarget, IClientWorkspaceContextService as IWorkspaceContextService } from '../../interfaces';
import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
export declare function getSettingsTargetName(target: ConfigurationTarget, resource: URI, workspaceContextService: IWorkspaceContextService): string;
export declare const PreDefinedSettingsURI: {
    Default: URI;
    User: URI;
    Workspace: URI;
    WorkspaceFolder: URI;
};
