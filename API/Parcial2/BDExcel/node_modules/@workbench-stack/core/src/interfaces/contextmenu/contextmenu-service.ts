import { IContextMenu_input, IActionRun_input } from '../generated-model';

export interface IContextMenuService {
    showContextMenu(delegate: IContextMenu_input): Promise<boolean>;
    hideContextMenu(): Promise<boolean>;
    runMenuAction(argument: IActionRun_input): Promise<boolean>;
}
