import * as network from '@vscode-alt/monaco-editor/esm/vs/base/common/network';
export declare namespace CdeCodeSchemas {
    /**
     * A schema that is used for setting files
     */
    const cdecode: string;
    const cdecodeRemote: string;
    const mongoDbFile: string;
    const cdebaseSettings: string;
}
export declare const Schemas: (typeof CdeCodeSchemas & typeof network.Schemas);
