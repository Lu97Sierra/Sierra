import { IEditorInput, ISideBySideEditorInput, IDiffEditorInput } from '../../interfaces';
import { CacheTypenames } from '../../constants';
import { URI } from 'cdecode/client';
export declare function generateSideBySideEditorInput(name: any, description: any, _details: IEditorInput, _master: IEditorInput): ISideBySideEditorInput;
export declare function generateDiffEditorInput(name: string, description: string | null, original: IEditorInput, modified: IEditorInput, forceOpenAsBinary?: boolean): IDiffEditorInput;
export declare function generateResourceEditorInput(name: string, description: string | null, resource: URI, preferredMode: string | undefined): {
    name: string;
    description: string;
    resource: URI;
    __typename: CacheTypenames;
};
