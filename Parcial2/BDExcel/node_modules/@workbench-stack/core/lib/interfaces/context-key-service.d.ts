import { IContextKeyService as IVscodeContextKeyService, IContextKey } from '@vscode-alt/monaco-editor/esm/vs/platform/contextkey/common/contextkey';
import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
export interface IContextKeyService extends Pick<IVscodeContextKeyService, 'createKey' | 'getContext' | 'getContextKeyValue'> {
    setContext<T>(key: string, value: any): void;
    removeContext(key: string): void;
}
export interface IResourceContextKey extends IContextKey<URI> {
}
