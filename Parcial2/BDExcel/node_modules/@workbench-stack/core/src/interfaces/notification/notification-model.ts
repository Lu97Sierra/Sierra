import { INotification_input, INotificationChangeEvent, INotificationViewItem } from '../generated-model';
import { Event } from '@vscode-alt/monaco-editor/esm/vs/base/common/event';
import { INotificationHandle } from './notification-service';

export interface INotificationsModel {

    readonly notifications: INotificationViewItem[];
    readonly onDidNotificationChange: Event<INotificationChangeEvent>;

    notify(notification: INotification_input): INotificationHandle;
}
