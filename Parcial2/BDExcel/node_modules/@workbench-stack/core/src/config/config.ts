// import * as envalid from 'envalid';


// const { str } = envalid;

// export const config = envalid.cleanEnv(process.env, {
//     NODE_ENV: str({ default: 'production', choices: ['production', 'staging', 'development', 'test'] }),
//     FOLDER_CONFIG_FOLDER_NAME: str({default: '.cdecode', desc: 'folder name for settings'}),
//     FOLDER_SETTINGS_NAME: str({default: 'settings', desc: 'folder settings name'}),
// });
const env = process['APP_ENV'] || process.env;

export const config = {
    NODE_ENV: env.NODE_ENV || 'development',
    FOLDER_CONFIG_FOLDER_NAME: env.FOLDER_CONFIG_FOLDER_NAME || '.cdecode',
    FOLDER_SETTINGS_NAME: env.FOLDER_SETTINGS_NAME || 'settings',
}
