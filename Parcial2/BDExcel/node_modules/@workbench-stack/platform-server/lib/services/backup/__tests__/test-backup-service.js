// import * as path from 'path';
// import { URI as Uri } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
// import { BackupMainService } from '../services';
// class TestBackupMainService extends BackupMainService {
//     constructor(backupHome: string, backupWorkspacesPath: string, configService: TestConfigurationService, logger) {
//         super(environmentService, configService, logger);
//         this.backupHome = backupHome;
//         this.workspacesJsonPath = backupWorkspacesPath;
//         // Force a reload with the new paths
//         this.loadSync();
//     }
//     public loadSync(): void {
//         super.loadSync();
//     }
//     public toBackupPath(arg: Uri | string): string {
//         const id = arg instanceof Uri ? super.getFolderHash(arg) : arg;
//         return path.join(this.backupHome, id);
//     }
//     public getFolderHash(folderUri: Uri): string {
//         return super.getFolderHash(folderUri);
//     }
//     public toLegacyBackupPath(folderPath: string): string {
//         return path.join(this.backupHome, super.getLegacyFolderHash(folderPath));
//     }
// }
//# sourceMappingURL=test-backup-service.js.map