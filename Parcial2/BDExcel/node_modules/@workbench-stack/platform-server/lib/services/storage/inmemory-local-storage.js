"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.InMemoryLocalStorage = void 0;
const types = require("@vscode-alt/monaco-editor/esm/vs/base/common/types");
class InMemoryLocalStorage {
    constructor() {
        this.store = {};
    }
    get length() {
        return Object.keys(this.store).length;
    }
    key(index) {
        const keys = Object.keys(this.store);
        if (keys.length > index) {
            return keys[index];
        }
        return null;
    }
    clear() {
        this.store = {};
    }
    setItem(key, value) {
        this.store[key] = value.toString();
    }
    getItem(key) {
        const item = this.store[key];
        if (!types.isUndefinedOrNull(item)) {
            return item;
        }
        return null;
    }
    removeItem(key) {
        delete this.store[key];
    }
}
exports.InMemoryLocalStorage = InMemoryLocalStorage;
//# sourceMappingURL=inmemory-local-storage.js.map