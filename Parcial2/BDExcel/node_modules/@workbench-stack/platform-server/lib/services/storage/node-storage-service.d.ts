import { IEnvironmentService } from '@workbench-stack/core';
export interface ISimpleStorageService {
    getItem<T>(key: string, defaultValue?: T): T;
    setItem(key: string, data: any): void;
    removeItem(key: string): void;
}
export declare class NodeStorageService implements ISimpleStorageService {
    private environmentService;
    private dbPath;
    private database;
    constructor(environmentService: IEnvironmentService);
    getItem<T>(key: string, defaultValue?: T): T;
    setItem(key: string, data: any): void;
    removeItem(key: string): void;
    private load;
    private save;
}
