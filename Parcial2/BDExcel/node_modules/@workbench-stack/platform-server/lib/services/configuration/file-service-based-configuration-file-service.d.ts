import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
import { IDisposable } from '@vscode-alt/monaco-editor/esm/vs/base/common/lifecycle';
import { IFileService } from '@files-stack/core';
import { IConfigurationFileService } from '../../interfaces';
export declare class ConfigurationFileService implements IConfigurationFileService {
    fileService: IFileService;
    constructor(fileService: IFileService);
    get onFileChanges(): any;
    readonly whenWatchingStarted: Promise<void>;
    readonly isWatching: boolean;
    whenProviderRegistered(scheme: string): Promise<void>;
    watch(resource: URI): IDisposable;
    exists(resource: URI): Promise<boolean>;
    readFile(resource: URI): Promise<string>;
}
