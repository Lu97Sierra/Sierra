import { Container } from 'inversify';
import { Service, ServiceBroker } from 'moleculer';
import { IClientConfigurationService } from '@workbench-stack/core';
export declare class ConfigurationClientMoleculerService extends Service {
    private _configurationService;
    private container;
    constructor(broker: ServiceBroker, { container, settings }: {
        container: Container;
    } & {
        settings: any;
    });
    get configurationService(): IClientConfigurationService | Promise<IClientConfigurationService>;
}
