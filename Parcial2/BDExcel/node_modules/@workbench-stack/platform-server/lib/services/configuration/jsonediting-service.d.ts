import { IJSONEditingService, IJSONValue } from '@workbench-stack/core';
import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
import { IFileService, INodeTextFileService } from '@files-stack/core';
import * as ILogger from 'bunyan';
export declare class JSONEditingService implements IJSONEditingService {
    private fileService;
    private textFileService;
    private logger;
    _serviceBrand: any;
    private queue;
    constructor(fileService: IFileService, textFileService: INodeTextFileService, logger: ILogger);
    write(resource: URI, value: IJSONValue, save: boolean): Promise<void>;
    private doWriteConfiguration;
    private writeToBuffer;
    private applyEditsToBuffer;
    private getEdits;
    private resolveModelReference;
    private resolveAndValidate;
    private reject;
    private toErrorMessage;
}
