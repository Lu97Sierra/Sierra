"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.testFile = void 0;
/* tslint:disable */
const uuid_1 = require("@vscode-alt/monaco-editor/esm/vs/base/common/uuid");
const path_1 = require("path");
const os_1 = require("os");
const pfs_1 = require("@vscode-alt/monaco-editor/esm/vs/base/node/pfs");
function testFile(folder, file) {
    const id = uuid_1.generateUuid();
    const parentDir = path_1.join(os_1.tmpdir(), 'vsctests', id);
    const newDir = path_1.join(parentDir, 'config', id);
    const testFile = path_1.join(newDir, 'config.json');
    return pfs_1.mkdirp(newDir, 493).then(() => {
        return {
            testFile,
            cleanUp: () => pfs_1.rimraf(parentDir, pfs_1.RimRafMode.MOVE)
        };
    });
}
exports.testFile = testFile;
//# sourceMappingURL=test-utils.js.map