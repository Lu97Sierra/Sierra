import { INotificationHandle, NotificationMessage, INotificationActions, INotificationProgress, NotificationSeverity as Severity } from '@workbench-stack/core';
import { Event } from '@vscode-alt/monaco-editor/esm/vs/base/common/event';
export declare class NoOpNotification implements INotificationHandle {
    readonly progress: NoOpProgress;
    private readonly _onDidClose;
    get onDidClose(): Event<void>;
    updateSeverity(severity: Severity): void;
    updateMessage(message: NotificationMessage | any): void;
    updateActions(actions?: INotificationActions): void;
    close(): void;
}
export declare class NoOpProgress implements INotificationProgress {
    infinite(): void;
    done(): void;
    total(value: number): void;
    worked(value: number): void;
}
