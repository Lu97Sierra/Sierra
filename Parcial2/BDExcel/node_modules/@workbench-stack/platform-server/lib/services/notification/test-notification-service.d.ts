import { INotification, INotificationHandle, NotificationSeverity as Severity, IPromptChoice, IPromptOptions } from '@workbench-stack/core';
export declare class TestNotificationService /** implements INotificationService */ {
    _serviceBrand: any;
    private static readonly NO_OP;
    info(message: string | any): INotificationHandle;
    warn(message: string | any): INotificationHandle;
    error(error: string | Error | any): INotificationHandle;
    notify(notification: INotification): INotificationHandle;
    prompt(severity: Severity, message: string, choices: IPromptChoice[], options?: IPromptOptions): INotificationHandle;
}
