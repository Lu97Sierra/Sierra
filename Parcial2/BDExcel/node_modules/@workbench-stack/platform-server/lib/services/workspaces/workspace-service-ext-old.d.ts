import { IWorkspaceContextService, Disposable } from '@workbench-stack/core';
import { ServiceBroker } from 'moleculer';
export declare class WorkspaceServiceExtOld extends Disposable {
    private workspaceContextService;
    private broker;
    private pubsub;
    constructor(workspaceContextService: IWorkspaceContextService, broker: ServiceBroker, pubsub: any);
    private registerListeners;
    private onBrodcastChangeWorkbenchState;
    private onBrodcastChangeWorkspaceName;
    private onBroadcastChangeWorkspaceFolders;
}
