import { IURLService, IURLHandler } from '../../interfaces';
import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
import { IDisposable } from '@vscode-alt/monaco-editor/esm/vs/base/common/lifecycle';
export declare class URLService implements IURLService {
    private handlers;
    open(uri: URI): Promise<boolean>;
    registerHandler(handler: IURLHandler): IDisposable;
}
export declare class RelayURLService extends URLService implements IURLHandler {
    private urlService;
    constructor(urlService: IURLService);
    open(uri: URI): Promise<boolean>;
    handleURL(uri: URI): Promise<boolean>;
}
