import { IConfigurationCache, IConfigurationKey as ConfigurationKey, IEnvironmentService } from '@workbench-stack/core';
export declare class ConfigurationCache implements IConfigurationCache {
    private readonly environmentService;
    private readonly cachedConfigurations;
    constructor(environmentService: IEnvironmentService);
    read(key: ConfigurationKey): Promise<string>;
    write(key: ConfigurationKey, content: string): Promise<void>;
    remove(key: ConfigurationKey): Promise<void>;
    private getCachedConfiguration;
}
