import { Disposable } from '@vscode-alt/monaco-editor/esm/vs/base/common/lifecycle';
import { Event } from '@vscode-alt/monaco-editor/esm/vs/base/common/event';
import { ConfigurationModel } from '@workbench-stack/core';
export declare class NodeBasedUserConfiguration extends Disposable {
    private settingsPath;
    private userConfigModelWatcher;
    private initializePromise;
    private readonly _onDidChangeConfiguration;
    readonly onDidChangeConfiguration: Event<ConfigurationModel>;
    constructor(settingsPath: string);
    initialize(): Promise<ConfigurationModel>;
    initializeSync(): ConfigurationModel;
    reload(): Promise<ConfigurationModel>;
    getConfigurationModel(): ConfigurationModel;
}
