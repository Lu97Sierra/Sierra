import { IContextViewService, IContextViewDelegate } from './contextView';
import { ContextViewDOMPosition } from '../../../base/browser/ui/contextview/contextview';
import { Disposable, IDisposable } from '../../../base/common/lifecycle';
import { ILayoutService } from '../../layout/browser/layoutService';
export declare class ContextViewService extends Disposable implements IContextViewService {
    readonly layoutService: ILayoutService;
    readonly _serviceBrand: undefined;
    private currentViewDisposable;
    private contextView;
    private container;
    constructor(layoutService: ILayoutService);
    setContainer(container: HTMLElement, domPosition?: ContextViewDOMPosition): void;
    showContextView(delegate: IContextViewDelegate, container?: HTMLElement, shadowRoot?: boolean): IDisposable;
    getContextViewElement(): HTMLElement;
    layout(): void;
    hideContextView(data?: any): void;
}
