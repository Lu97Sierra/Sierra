import { CancellationToken } from '../../../base/common/cancellation';
import { ITextModel } from '../../common/model';
import { CodeLensProvider, CodeLens, CodeLensList } from '../../common/modes';
export interface CodeLensItem {
    symbol: CodeLens;
    provider: CodeLensProvider;
}
export declare class CodeLensModel {
    lenses: CodeLensItem[];
    private readonly _disposables;
    dispose(): void;
    add(list: CodeLensList, provider: CodeLensProvider): void;
}
export declare function getCodeLensModel(model: ITextModel, token: CancellationToken): Promise<CodeLensModel>;
