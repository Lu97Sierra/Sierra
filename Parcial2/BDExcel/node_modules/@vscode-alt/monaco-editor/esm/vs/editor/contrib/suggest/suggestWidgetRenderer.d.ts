import { DisposableStore } from '../../../base/common/lifecycle';
import { IListRenderer } from '../../../base/browser/ui/list/list';
import { ICodeEditor } from '../../browser/editorBrowser';
import { CompletionItem } from './suggest';
import { IThemeService, ThemeIcon } from '../../../platform/theme/common/themeService';
import { IModeService } from '../../common/services/modeService';
import { IconLabel } from '../../../base/browser/ui/iconLabel/iconLabel';
import { IModelService } from '../../common/services/modelService';
import { Event } from '../../../base/common/event';
export declare function getAriaId(index: number): string;
export declare const suggestMoreInfoIcon: ThemeIcon;
export interface ISuggestionTemplateData {
    root: HTMLElement;
    /**
     * Flexbox
     * < ------------- left ------------ >     < --- right -- >
     * <icon><label><signature><qualifier>     <type><readmore>
     */
    left: HTMLElement;
    right: HTMLElement;
    icon: HTMLElement;
    colorspan: HTMLElement;
    iconLabel: IconLabel;
    iconContainer: HTMLElement;
    parametersLabel: HTMLElement;
    qualifierLabel: HTMLElement;
    /**
     * Showing either `CompletionItem#details` or `CompletionItemLabel#type`
     */
    detailsLabel: HTMLElement;
    readMore: HTMLElement;
    disposables: DisposableStore;
}
export declare class ItemRenderer implements IListRenderer<CompletionItem, ISuggestionTemplateData> {
    private readonly _editor;
    private readonly _modelService;
    private readonly _modeService;
    private readonly _themeService;
    private readonly _onDidToggleDetails;
    readonly onDidToggleDetails: Event<void>;
    readonly templateId = "suggestion";
    constructor(_editor: ICodeEditor, _modelService: IModelService, _modeService: IModeService, _themeService: IThemeService);
    dispose(): void;
    renderTemplate(container: HTMLElement): ISuggestionTemplateData;
    renderElement(element: CompletionItem, index: number, data: ISuggestionTemplateData): void;
    disposeTemplate(templateData: ISuggestionTemplateData): void;
}
