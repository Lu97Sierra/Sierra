import { ISaveOptions, ILoadOptions } from './type-redirects';
import { URI } from '@vscode-alt/monaco-editor/esm/vs/base/common/uri';
export interface ITextFileEditorModel {
    updatePreferredEncoding(resource: URI, encoding: string): void;
    save(resource: URI, versionId: number, options?: ISaveOptions): Promise<void>;
    load(resource: URI, options?: ILoadOptions): Promise<any>;
    isDirty(resource: URI): boolean;
    isResolved(resource: URI): boolean;
}
